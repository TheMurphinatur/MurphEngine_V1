#pragma once

#include "ActorManager.h"
#include "Message.h"

namespace MurphEngine
{

    class MessageSystem
    {

        //broadcast event to everything in game
        //void SendMessage(Message event) const { m_pOwner->SendMessage(event); }

    };
}

//create a manager to keep track of every actor
//
// broadcast event to all objects in the object manager.